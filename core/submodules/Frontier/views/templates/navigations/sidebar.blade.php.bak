@foreach ($menus as $parent => $menu)
        <!-- if -->
        @if (isset($menu->is_header) && $menu->is_header)

            <v-list-tile>
                <v-layout
                    row
                    align-center
                >
                    <v-flex xs6>
                        <v-subheader>{{ $menu->text }}</v-subheader>
                    </v-flex>
                </v-layout>
            </v-list-tile>

        <!-- else if -->
        @elseif (isset($item->divider) && $item->divider)

            <v-divider :dark.sync="dark" :light.sync="light" class="my-4"></v-divider>

        <!-- else -->
        @else

            @include("Frontier::templates.navigations.nav-item", ['menu' => collect($menu), 'depth' => ++$depth])

            @if ($menu->has_children)
            <v-list-group no-action>
            @endif


            @if ($menu->has_children)
            </v-list-group>
            @endif
        @endif
@endforeach
