Routing:
  - Added namespace on routes - avoid using the Qualified name namespace on your routes. E.g. use 'QuestController' instead of 'Quest\Controllers\QuestController'.
                    - if you must override the namespace use a Fully qualified name. E.g. use '\Quest\Controllers\QuestController'.

  Fuzzy:
  - Removed the route core







====================
Non-Breaking Changes
====================
Config:
  ☐ added _config/download.php_, will replace _config/downloadables.php_
